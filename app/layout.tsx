import type { Metadata, Viewport } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Daniel Fuentes - Software Engineer Portfolio",
  description: "Daniel Fuentes is a Mexican-American software engineer from El Paso, Texas. View his projects, experience, and get in touch for collaboration opportunities.",
  keywords: ["software engineer", "web developer", "portfolio", "Daniel Fuentes", "El Paso", "UTEP"],
  authors: [{ name: "Daniel Fuentes" }],
  openGraph: {
    title: "Daniel Fuentes - Software Engineer Portfolio",
    description: "Software engineer passionate about technology and community",
    type: "website",
  },
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
